{
  "name": "mernchat",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "concurrently \"node server --ignore client\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build",
    "install": "cd server && npm i && cd ../client && npm i",
    "develop": "concurrently \"node server --ignore client\" \"cd client && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "serve": "^14.2.1"
  }
}
